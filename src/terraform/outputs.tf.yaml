output:
  project_id:
    value: ${random_id.project_id.hex}
    description: The project ID.
  ip_address:
    value: ${module.vm_instance.ip_address.address}
    description: The IP address of the Minecraft server.
